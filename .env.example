# Google Drive Shared Drive Folder ID
# Get this from the URL: https://drive.google.com/drive/folders/{FOLDER_ID}
GOOGLE_DRIVE_FOLDER_ID=your-folder-id-here

# Paths to sync (comma-separated)
# Can be files or directories
SYNC_PATHS=docs,PLANNING.md,TASK.md,README.md

# Enable Mermaid diagram rendering (true/false)
# When enabled, ```mermaid blocks are rendered as PNG images and embedded
ENABLE_MERMAID=true

# Rate limiting configuration
# Delay between API calls in seconds (increase for large syncs)
RATE_LIMIT_DELAY=0.5

# Batch size for cache saves
# Cache is saved every N files to prevent data loss on errors
BATCH_SIZE=10
